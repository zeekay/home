
import React from 'react';
import Terminal from './Terminal';

const TerminalContent: React.FC = () => {
  return <Terminal className="w-full h-full rounded-none" />;
};

export default TerminalContent;
